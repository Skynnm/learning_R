# 向量（vector）是用于存储单一数据类型的一维数组。
# 执行组合功能的函数c()可用来创建向量。catenate, collect, combine

# 代码清单2-0-1：向量
a <- c(1, 2, 5, 3, 6, -2, 4)
b <- c("one", "two", "three")
c <- c(TRUE, TRUE, TRUE, FALSE, TRUE, FALSE)
