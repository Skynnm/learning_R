贵在坚持
量变产生质变
面向目标编程
R base 和 tidyverse 分开学
核心思想：R原生擅长对整列操作，尽量不要写for循环。坏例子：逐个遍历；好例子：df$score*2
【逻辑测试、取子集、元素方式执行】

R闭包 = 一个函数 + 它出生时绑定的外层私有环境。即使外层函数跑完消失，内层函数依然能读写那个环境里的变量。

# 利用下面3个简单的策略就可以简化这个任务以及其他同样复杂的编程任务
#（1）将复杂的任务分解为一些简单的子任务
#【有序步骤（sequential step） 和 同类情况（parallel case）】
#（2）使用实例
#（3）用通俗的语言描述解决方案，然后将其转换成 R 代码


【在R中存储信息并非只能通过赋值的方式；创建某种特殊的行为也不一定只能通过编写函数来实现。】这两个任务都可以通过R的S3系统来完成。
【这个系统是通过泛型函数来实现其功能的。这些泛型函数会检查其输入对象的类属性，并有针对性地生成基于类属性的输出结果】
许多S3方法都会寻找并使用存储在对象属性中的额外信息
